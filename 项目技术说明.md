# 恶劣天气图像复原系统 - 技术说明文档

## 一、项目概述

本项目实现了一个面向恶劣天气（雨、雾、雪）的图像复原系统，核心采用**条件扩散模型 (WeatherDiffusion)**，同时提供传统算法基线（暗通道先验 DCP、导向滤波分层）用于对照实验。系统包含完整的推理引擎、批量评测框架和 PyQt5 可视化界面。

**核心能力：**
- 基于扩散模型的单图/批量天气复原（需 GPU + 预训练权重）
- 传统算法复原（纯 CPU，无需权重，开箱即用）
- 自动退化类型判别（雨/雾/雪）
- PSNR/SSIM/LPIPS/NIQE 等多指标评测
- 可视化对比界面

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户界面层 (ui/)                         │
│  MainWindow / SinglePage / BatchPage / EnvPage / CompareView    │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                       推理编排层 (engine/)                       │
│  ModelCache (权重缓存) / restore_array / restore_batch          │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                        核心算法层 (core/)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ BaseRestorer │  │   Registry   │  │  WeatherDiffRestorer │  │
│  │  (统一接口)  │  │  (动态注册)  │  │  (扩散模型复原器)    │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │ DCP Restorer │  │ Classical    │  │   Config / Metrics   │  │
│  │ (暗通道去雾) │  │ (分层+尖峰)  │  │   (配置/指标)        │  │
│  └──────────────┘  └──────────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、核心算法原理

### 3.1 扩散模型基础 (WeatherDiffusion)

扩散模型的核心思想是：前向过程逐步向图像添加高斯噪声直至变成纯噪声，反向过程学习从噪声中恢复出清晰图像。

**数学描述：**

前向过程（加噪）：
```
q(x_t | x_{t-1}) = N(x_t; √(1-β_t) x_{t-1}, β_t I)
```

其中 β_t 是噪声调度系数，从 β_1=0.0001 线性增长到 β_T=0.02。

反向过程（去噪）：
```
p_θ(x_{t-1} | x_t) = N(x_{t-1}; μ_θ(x_t, t), Σ_θ(x_t, t))
```

**条件扩散：** 本项目的创新点在于将退化图像作为条件输入，模型学习的是"给定退化图，预测清晰图"的条件分布。

对应代码 `core/weatherdiff/sampling.py`：
```python
# beta 调度生成
def get_beta_schedule(beta_schedule, beta_start, beta_end, num_diffusion_timesteps):
    if beta_schedule == "linear":
        betas = np.linspace(beta_start, beta_end, num_diffusion_timesteps)
    return betas

# alpha_bar 计算（累积噪声系数）
def compute_alpha(beta, t):
    beta = torch.cat([torch.zeros(1).to(beta.device), beta], dim=0)
    return (1 - beta).cumprod(dim=0).index_select(0, t + 1).view(-1, 1, 1, 1)
```

### 3.2 Patch-based DDIM 采样

直接对整张高分辨率图做扩散推理显存消耗巨大。WeatherDiffusion 采用**分块推理 (Patch-based)** 策略：

1. 将整图切成 64×64 的重叠小块（步长 grid_r=16） 
2. 对每个 patch 独立预测噪声
3. 用覆盖次数 mask 做加权平均，得到整图噪声估计
4. 执行一步 DDIM 更新，循环直到完成所有采样步

对应代码 `core/weatherdiff/sampling.py` 中的 `generalized_steps_overlapping()`：
```python
def generalized_steps_overlapping(x, x_cond, seq, model, betas, ...):
    # 条件图变换到 [-1, 1]
    x_cond_neg = data_transform(x_cond)
    
    # 覆盖次数 mask (1, 1, H, W)
    x_grid_mask = torch.zeros_like(x[:, :1])
    for (hi, wi) in corners:
        x_grid_mask[:, :, hi:hi+patch_size, wi:wi+patch_size] += 1
    
    for step_idx, (t, next_t) in enumerate(zip(reversed(seq), reversed(seq_next))):
        # 1) 按 corners 切 patch，预测噪声并累加
        et_output = torch.zeros_like(x)
        for i in range(0, len(corners), patch_batch_size):
            batch_corners = corners[i:i+patch_batch_size]
            # 组装 batch: (n_patches, 6, patch_size, patch_size)
            # 前3通道: 条件图, 后3通道: 当前噪声图
            ...
            et_patch = model(patch_batch, t_batch)
            # 累加到整图
            for j, (hi, wi) in enumerate(batch_corners):
                et_output[:, :, hi:hi+patch_size, wi:wi+patch_size] += et_patch[j:j+1]
        
        # 2) 按覆盖次数平均
        et = et_output / x_grid_mask
        
        # 3) DDIM 更新公式
        x0_t = (x - et * (1 - at).sqrt()) / at.sqrt()
        xt_next = at_next.sqrt() * x0_t + (1 - at_next).sqrt() * et
        x = xt_next
```

**DDIM 加速采样：** 原始扩散需要 1000 步，DDIM 通过等间隔跳跃实现加速：
```python
def make_timestep_seq(num_diffusion_timesteps, sampling_timesteps):
    skip = num_diffusion_timesteps // sampling_timesteps
    return list(range(0, num_diffusion_timesteps, skip))
# 默认 1000 步压缩为 25 步
```

### 3.3 UNet 网络结构

扩散模型的去噪网络采用 ScoreNet 架构的 UNet，关键组件：

| 组件 | 作用 | 代码位置 |
|------|------|----------|
| Sinusoidal 时间嵌入 | 将时间步 t 编码为向量 | `unet.py: get_timestep_embedding()` |
| ResnetBlock | 带时间条件注入的残差块 | `unet.py: ResnetBlock` |
| AttnBlock | 自注意力机制（非局部） | `unet.py: AttnBlock` |
| Downsample/Upsample | 2× 下/上采样 | `unet.py: Downsample/Upsample` |

**网络配置（来自 `configs/allweather.yaml`）：**
```yaml
model:
  ch: 128              # 基础通道数
  ch_mult: [1, 2, 3, 4]  # 各层通道倍率
  num_res_blocks: 2    # 每层残差块数
  attn_resolutions: [16]  # 16×16 分辨率处使用注意力
  in_channels: 3       # 输入3通道（条件图）
  out_ch: 3            # 输出3通道（预测噪声）
```

**前向传播流程：**
```
输入: x=(B, 6, P, P) [条件图3通道 + 噪声图3通道], t=(B,)
  ↓
时间嵌入: t → sinusoidal → MLP → temb
  ↓
编码器: conv_in → [ResBlock×2 + Down]×4 → 多尺度特征栈
  ↓
Bottleneck: ResBlock → Attn → ResBlock
  ↓
解码器: [Concat skip + ResBlock×3 + Up]×4
  ↓
输出: conv_out → (B, 3, P, P) 预测噪声
```

### 3.4 暗通道先验去雾 (DCP)

DCP 是基于大气散射模型的经典去雾方法，不需要训练权重。

**大气散射模型：**
```
I(x) = J(x) · t(x) + A · (1 - t(x))
```
- I(x): 观测到的退化图像
- J(x): 需要恢复的清晰图像
- t(x): 透射率图（越远雾越浓，t 越小）
- A: 全局大气光

**暗通道先验原理：** 清晰自然图像的局部 patch 中，至少有一个颜色通道的值非常小（接近0）。雾图的暗通道值会明显偏高。

对应代码 `core/baseline/dcp.py`：
```python
def _dark_channel(img, patch):
    """通道最小 + 局部最小值滤波"""
    min_c = img.min(axis=2)  # 先取通道最小
    # 再取邻域最小（滑窗实现）
    ...
    return dark

def _atmospheric_light(img, dark):
    """取暗通道最亮 0.1% 像素中，原图亮度最高者作为大气光 A"""
    idx = np.argpartition(dark.ravel(), -n)[-n:]
    ...

# 复原公式: J = (I - A) / t + A
out = (img - atmos) / trans + atmos
```

### 3.5 传统分层复原方法

`ClassicalRestorer` 采用**导向滤波分层 + 尖峰抑制**策略，同时处理雨/雾/雪：

```
输入退化图
  ↓
[1] 自动判别退化类型 (detect_profile)
    - 暗通道均值高 → 雾
    - detail层垂直连通性 > 水平 → 雨
    - 否则 → 雪
  ↓
[2] 可选去雾 (大气散射模型)
  ↓
[3] 导向滤波分层
    base = GuidedFilter(gray, img)   # 保边平滑，保留结构
    detail = img - base              # 纹理层，含雨纹/雪花
  ↓
[4] detail层尖峰抑制
    detail = np.where(detail > median(detail) + τ, median(detail), detail)
    # 雨纹/雪花是亮尖峰，用中值替换
  ↓
[5] 第二轮更大窗口清理粗雪花/宽雨纹
  ↓
[6] 轻微锐化恢复细节
  ↓
输出复原图
```

对应代码 `core/baseline/classical.py`：
```python
# 导向滤波分层
base = guided_filter(gray, img, radius=radius, eps=eps)
detail = img - base

# detail层去尖峰
detail_med = median_filter(detail, med_size)
detail = np.where(detail > detail_med + tau, detail_med, detail)

# 合成
restored = np.clip(base + detail, 0.0, 1.0)
```

---

## 四、关键设计模式

### 4.1 统一接口契约 (BaseRestorer)

所有复原算法（扩散模型、传统方法）都继承 `BaseRestorer`，UI 和评测层只依赖接口，不关心底层实现：

```python
# core/base.py
class BaseRestorer(abc.ABC):
    @abc.abstractmethod
    def load_model(self) -> None: ...
    
    @abc.abstractmethod
    def restore(self, image, progress_cb, cancel_cb) -> np.ndarray: ...
    
    def restore_with_stats(self, image, ...) -> RestoreResult:
        # 统一计时、验证、封装结果
        ...
```

**约定：**
- 图像格式：`numpy.ndarray, dtype=uint8, shape=(H, W, 3), RGB`
- 输出尺寸必须与输入一致
- 支持进度回调和取消回调

### 4.2 延迟导入与模型注册

`registry.py` 实现了按需加载机制：

```python
# 延迟导入映射（避免启动就加载 torch）
_LAZY_MODULES = {
    "weatherdiff": "core.weatherdiff.restorer",
    "dcp": "core.baseline.dcp",
    "classical": "core.baseline.classical",
}

def build_restorer(config):
    name = config.get("restorer")
    _ensure_loaded(name)  # 按需 import，触发 @register_restorer
    cls = _RESTORERS[name]
    return cls(config)
```

**好处：**
- 传统方法完全不依赖 torch
- 切换天气类型时只加载对应模型
- 新增算法只需 `@register_restorer("name")` 装饰器

### 4.3 权重缓存 (ModelCache)

界面切换天气类型时不会重复加载权重：

```python
# engine/inference.py
class ModelCache:
    def get(self, weather, overrides=None):
        key = self._make_key(weather, overrides)
        if key not in self._cache:
            restorer = build_restorer(cfg)
            restorer.load_model()
            self._cache[key] = restorer
        return self._cache[key]
```

---

## 五、代码结构说明

```
project/
├── app.py                    # UI 启动入口
├── configs/                  # YAML 配置文件
│   ├── allweather.yaml       # 统一模型配置
│   ├── rain.yaml             # 雨专用配置
│   ├── haze.yaml             # 雾专用配置
│   ├── snow.yaml             # 雪专用配置
│   ├── dcp.yaml              # DCP 配置（无需权重）
│   └── classical.yaml        # 传统方法配置（无需权重）
├── core/                     # 核心算法
│   ├── base.py               # BaseRestorer 统一接口
│   ├── registry.py           # 模型注册表
│   ├── config.py             # 配置加载
│   ├── image_utils.py        # 图像 IO 与滤波工具
│   ├── metrics.py            # 评测指标 (PSNR/SSIM/LPIPS/NIQE)
│   ├── synth.py              # 合成样本生成
│   ├── weatherdiff/          # 扩散模型实现
│   │   ├── restorer.py       # 复原器（权重加载/推理编排）
│   │   ├── unet.py           # UNet 网络结构
│   │   └── sampling.py       # Patch-based DDIM 采样
│   └── baseline/             # 传统算法基线
│       ├── dcp.py            # 暗通道先验去雾
│       └── classical.py      # 导向滤波分层 + 尖峰抑制
├── engine/                   # 推理编排
│   ├── inference.py          # 单图/批量推理
│   └── evaluate.py           # 批量评测与 CSV 导出
├── ui/                       # PyQt5 界面
│   ├── main_window.py        # 主窗口（三个 Tab）
│   ├── panels.py             # 单图/批量/环境页面
│   ├── compare_view.py       # 图像对比视图
│   ├── worker.py             # 后台推理线程
│   └── theme.py              # 主题样式
├── scripts/                  # 命令行工具
│   ├── demo.py               # 一键演示
│   ├── run_eval.py           # 批量评测
│   ├── make_samples.py       # 生成合成样本
│   ├── download_weights.py   # 下载预训练权重
│   └── smoke_test.py         # 环境自检
├── data/                     # 数据目录
│   ├── samples/              # 合成样本（自动生成）
│   └── real_test/            # 真实测试图片
└── weights/                  # 预训练权重
    └── WeatherDiff64.pth.tar # WeatherDiffusion 权重
```

---

## 六、数据流与推理流程

### 6.1 单图推理流程

```
用户选择图片
    ↓
[1] 预处理 (preprocess_for_model)
    - 长边限制 max_side=1024
    - 尺寸对齐 16 的倍数
    - 小图反射填充到 patch_size
    ↓
[2] 条件图变换
    - uint8 → float32 [0,1]
    - [0,1] → [-1,1] (data_transform)
    ↓
[3] Patch-based DDIM 采样
    - 生成重叠 patch 坐标 (corner_list)
    - 循环 sampling_timesteps 次:
        - 对每个 patch 调用 UNet 预测噪声
        - 覆盖平均得到整图噪声估计
        - DDIM 更新公式
    ↓
[4] 后处理 (postprocess_to_origin)
    - [-1,1] → [0,1] → uint8
    - 裁剪/缩放到原始尺寸
    ↓
输出复原图
```

### 6.2 批量评测流程

```
输入目录 + GT目录
    ↓
match_pairs() 按文件名配对 (input, gt)
    ↓
遍历每张图:
    - load_image() 读取
    - restorer.restore_with_stats() 复原
    - compute_metrics() 计算指标
    - save_image() 保存结果
    ↓
summarize() 汇总均值
    ↓
write_csv() / write_summary_csv() 导出
```

---

## 七、配置系统

每个天气类型对应一个 YAML 配置，关键字段：

```yaml
# configs/allweather.yaml
restorer: weatherdiff          # 使用哪个复原器

weights:
  path: weights/WeatherDiff64.pth.tar

data:
  image_size: 64               # patch 大小
  max_side: 1024               # 推理前长边限制
  size_multiple: 16            # 尺寸对齐倍数

model:
  ch: 128                      # UNet 基础通道
  ch_mult: [1, 2, 3, 4]        # 多尺度通道倍率
  num_res_blocks: 2            # 每层残差块数
  attn_resolutions: [16]       # 注意力应用分辨率
  ema: true                    # 推理使用 EMA 权重

diffusion:
  beta_schedule: linear        # beta 调度类型
  num_diffusion_timesteps: 1000  # 总扩散步数

sampling:
  timesteps: 25                # DDIM 采样步数（加速）
  grid_r: 16                   # patch 滑窗步长
  eta: 0.0                     # DDIM eta（0=确定性）
  patch_batch_size: 32         # 并行 patch 数
  seed: 61                     # 随机种子
```

---

## 八、评测指标

| 指标 | 类型 | 依赖 | 说明 |
|------|------|------|------|
| PSNR | 有参考 | numpy | 峰值信噪比，越高越好 |
| SSIM | 有参考 | skimage(可选) | 结构相似度，越高越好 |
| LPIPS | 有参考 | torch, lpips | 感知相似度，越低越好 |
| NIQE | 无参考 | pyiqa | 自然度评估，越低越好 |
| BRISQUE | 无参考 | pyiqa | 图像质量评估，越低越好 |
| FID | 数据集级 | pytorch-fid | 分布距离，越低越好 |

---

## 九、快速使用

```bash
# 1. 环境自检
python scripts/smoke_test.py

# 2. 生成合成样本（无需数据集）
python scripts/make_samples.py

# 3. 命令行演示（传统方法，无需权重）
python scripts/demo.py

# 4. 下载预训练权重（扩散模型需要）
python scripts/download_weights.py

# 5. 打开可视化界面
python app.py
```

---

## 十、技术要点总结

1. **条件扩散模型**：以退化图为条件，学习清晰图分布，比传统方法更能处理复杂退化

2. **Patch-based 推理**：将显存消耗与图像分辨率解耦，支持任意尺寸输入

3. **DDIM 加速**：1000 步扩散压缩到 25 步，大幅减少推理时间

4. **EMA 权重**：指数移动平均参数，推理效果明显优于普通权重

5. **统一接口设计**：BaseRestorer 抽象使 UI/评测与算法解耦，便于扩展

6. **延迟导入**：torch 仅在使用扩散模型时加载，传统方法零依赖

7. **自动退化判别**：基于暗通道均值和 detail 层连通性分析，自动选择最优处理策略
